# 引継ぎ書類 - 2025-11-11

## 作業概要

**問題**: 項番号「①②③」の途中に「23」というページ番号が入ってしまう問題の修正

## 作業内容

### 実施した修正

#### 1. coordinate_ocr.py の修正（2回）

**第1回修正（コミット: 3ab6179）**
- `CoordinateOCR.__init__`に`debug`引数を追加（未定義エラーを修正）
- `remove_out_of_sequence_numbers`関数を改善
  - 丸数字（①②③）と普通の数字（23）のタイプを判定
  - 前後が丸数字で真ん中が普通の数字の場合は異常として除去

**第2回修正（コミット: 557434b）**
- `is_likely_page_number`関数を大幅改善
  - **新ロジック**: 10以上の数字は基本的にページ番号として扱う
  - 理由: 項番号は通常①-⑨、または(1)-(9)の範囲
  - 23, 24などはページ番号の可能性が高い
  - ただし、周囲に長いテキストがある場合は項番号として残す

#### 2. pdf_parser.py の修正

- `CoordinateOCR`初期化時に`debug`フラグを渡すよう修正

```python
self.coordinate_ocr = CoordinateOCR(debug=self.debug)
```

### 問題の詳細

**ユーザーからの報告内容**:
```
第22条(代休)
1. ...
2. ...
23  ← ここに「23」が入る（ページ番号）
3. 前項の代休は、休日勤務した日から1ヶ月以内の取得を原則とする。

第23条(1ヶ月単位の変形労働時間制)
1. ...
2. ...
23  ← ここにも「23」が入る（ページ番号）
3. 各変形期間の出勤日及び休日については...
```

**根本原因**:
- 「23」はページ番号
- PDFの改ページ位置でページ番号が項番号の途中に挟まってしまう
- 従来のロジックではページの上端・下端のみをページ番号と判定していたため、ページ中央にあるページ番号を検出できなかった

**修正内容（最終版）**:
- 周囲のコンテキストで判定
  - 「第」「条」「章」がある → 条番号として残す（例：第23条は残す）
  - 丸数字（①②③）の間にある → ページ番号として除去
  - 孤立している → ページ番号として除去

**誤った修正（557434b）**:
- 10以上の数字を除去 → 第23条、第80条なども消えてしまう（誤り）
- この修正は取り消し、正しいロジックに変更

## デプロイ状況

### コミット履歴

```
8c05a13 - ページ番号判定ロジックを修正（条番号を残す）(最新・正しい修正)
557434b - ページ番号「23」除去ロジックを改善 (誤り：10以上を除去)
3ab6179 - 項番号「23」異常検出の修正
1947ff8 - 項番号シーケンスチェック機能を追加
```

### デプロイ手順（DEPLOY_RULES.md参照）

1. ✅ コードをコミット・プッシュ済み
2. ⏳ **ビルド完了待ち** - gcloud認証エラーで確認不可
3. ⏳ **トラフィック切り替え待ち**
4. ⏳ **本番環境での動作確認待ち**

### 次のステップ（残作業）

```bash
# 1. ビルド状況を確認
gcloud builds list --limit=3

# 2. ビルドがSUCCESSになったら、最新リビジョンを取得
LATEST_REV=$(gcloud run revisions list --service=syuugyoukisoku --region=asia-northeast1 --limit=1 --format="value(metadata.name)")
echo "最新リビジョン: $LATEST_REV"

# 3. トラフィックを100%最新リビジョンに切り替え
gcloud run services update-traffic syuugyoukisoku --region=asia-northeast1 --to-revisions=$LATEST_REV=100

# 4. トラフィック設定を確認
gcloud run services describe syuugyoukisoku --region=asia-northeast1 --format="table(status.traffic)"

# 5. 本番環境で動作確認
# https://syuugyoukisoku-bsdy2np4aa-an.a.run.app
```

## 技術的な詳細

### 修正箇所（最終版）

**coordinate_ocr.py:177-245**
```python
def is_likely_page_number(self, block: Dict, all_blocks: List[Dict], page_height: int = 3000) -> bool:
    """
    ページ番号の可能性が高いかを判定（修正版）

    判定基準:
    1. 1-3桁の数字のみ
    2. 周囲のコンテキストで判定
       - 「第」「条」などがある → 条番号として残す
       - 丸数字（①②③）の間にある → ページ番号として除去
       - 孤立している → ページ番号として除去
    3. ページの上端・下端は優先的にページ番号として扱う
    """
    text = block['text'].strip()

    if not re.match(r'^\d{1,3}$', text):
        return False

    # 周囲50px以内のブロックを取得
    nearby_blocks = [...]
    nearby_texts = [b['text'].strip() for b in nearby_blocks]

    # 条番号の一部かどうかをチェック
    # 前後に「第」「条」「(」などがある場合は条番号として残す
    for nearby_text in nearby_texts:
        if re.search(r'第|条|（|章', nearby_text):
            return False  # 条番号・章番号の一部なので残す

    # 周囲に丸数字（項番号）がある場合
    # → その間に挟まった数字はページ番号の可能性が高い
    has_circle_numbers_nearby = any(
        re.search(r'[①-⑳]', nearby_text) for nearby_text in nearby_texts
    )

    if has_circle_numbers_nearby:
        # 丸数字のシーケンス中にある数字はページ番号の可能性が高い
        return True

    # 孤立している数字はページ番号
    # ... (以下略)
```

### テストの考慮事項

- `debug_23_issue.py`というデバッグスクリプトを追加（本番環境では使用しない）
- このスクリプトはローカルでのデバッグ用なので、デプロイには影響しない

## 注意事項

### デプロイルール（絶対遵守）

**DEPLOY_RULES.md**に記載の通り、以下を必ず守ること：

1. **ビルドが完了するまで待つ** - STATUS=SUCCESSを確認
2. **具体的なリビジョン名を指定** - `--to-latest`は使わない
3. **必ず手動でトラフィックを切り替える** - 自動では切り替わらない
4. **トラフィック設定を確認** - デプロイ後に必ず確認

### 確認が必要な問題

ユーザーから「文章が削除されている」という報告もあるため、以下を確認する必要がある：

1. 今回の修正で文章の削除は起きていないか？
2. `remove_out_of_sequence_numbers`が過剰に削除していないか？
3. 実際のPDFで「23」が正しく除去され、他のテキストは残っているか？

## アプリケーション情報

- **URL**: https://syuugyoukisoku-bsdy2np4aa-an.a.run.app
- **プロジェクトID**: ocr2-435601
- **サービス名**: syuugyoukisoku
- **リージョン**: asia-northeast1
- **GitHubリポジトリ**: https://github.com/naoota12345678/syuugyoukisoku.git

## 関連ファイル

```
coordinate_ocr.py       - OCR座標処理・ページ番号判定
pdf_parser.py          - PDF解析メイン処理
structure_analyzer.py  - テキスト構造解析（条・項の検出）
DEPLOY_RULES.md       - デプロイルール（絶対遵守）
DEPLOY.md             - デプロイ手順
debug_23_issue.py     - デバッグスクリプト（本番不使用）
```

## 推奨される次の作業

1. **ビルド完了を確認**してトラフィックを切り替える
2. **本番環境で「23」が除去されているか確認**
3. **「文章が削除されている」問題を確認**
   - もし文章が削除されていたら、`is_likely_page_number`の判定条件を緩和する必要がある
   - 例：周囲50pxの範囲を100pxに拡大、または長文の定義（50文字）を調整
4. 複数のPDFで動作確認を行う

## トラブルシューティング

### 「23」がまだ残っている場合

`coordinate_ocr.py:206-225`の判定条件を確認：
- 周囲50px以内にテキストがあると判定されている可能性
- 範囲を100pxに拡大、または判定ロジックを調整

### 正しいテキストが削除されている場合

`coordinate_ocr.py:206-225`の判定が厳しすぎる可能性：
- 「長文」の定義（50文字）を調整
- または、10以上の数字でも特定の条件では残すようにする

---

**作成日時**: 2025-11-11 15:30 (JST)
**作成者**: Claude Code
**最終コミット**: 557434b - ページ番号「23」除去ロジックを改善
